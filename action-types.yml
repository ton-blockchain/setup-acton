# See https://github.com/typesafegithub/github-actions-typing

inputs:
  acton-version:
    type: string
  github-token:
    type: string
  architecture:
    type: string
  platform:
    type: string

outputs:
  acton-path:
    type: string
